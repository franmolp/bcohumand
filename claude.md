# Instrucciones del proyecto

## Idioma
Siempre responder en español. Nunca usar inglés en los mensajes al usuario.

## Git
- La rama principal es `main`. Nunca hacer push a `master`.
- Siempre hacer push con: `git push -u origin main`
